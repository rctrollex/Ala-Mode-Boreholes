const message = encodeURIComponent(
  "Hello, I would like to request a quote for your Borehole Services."
);

export default message;

const inspectionMessage = encodeURIComponent(
  "Hello, I would like to book a site inspection for borehole drilling."
);

export { inspectionMessage };